A tool set we created to help setup our environments faster

This script assumes you are running as root.

Usage ./menu.py

We recommend making a symbolic link

ln -s /root/chips/menu.py /usr/local/bin/chips
